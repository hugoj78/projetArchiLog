<h1> All Mangas </h1>

<ul>
	<% @mangas.each do |manga| %>
			<li><%= link_to manga.name , manga_path(manga) %></li>
	<% end %>
</ul>
<% if policy(@mangas).new? %>
	<%= link_to "Add manga", new_manga_path %>
<% end %>

